% Just to test the function module 'scrape_user_animelist.m'

username = 'DarkMiR4GE';
user_animelist = scrape_user_animelist(username);
if isempty(user_animelist)
    disp('User AnimeList export failed')
    disp(['Make sure:' newline])
    disp(['
end
